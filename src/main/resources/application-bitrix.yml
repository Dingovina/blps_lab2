app:
  bitrix:
    enabled: true
    backfill-on-startup: ${BITRIX_BACKFILL_ON_STARTUP:true}
    rest-base-url: ${BITRIX_REST_BASE_URL:}
    polling-interval-seconds: ${BITRIX_POLLING_INTERVAL_SECONDS:20}
    deal-category-id: ${BITRIX_DEAL_CATEGORY_ID:0}
    deal-field-listing-id: ${BITRIX_DEAL_FIELD_LISTING_ID:UF_CRM_1779488860318}
    deal-field-promotion: ${BITRIX_DEAL_FIELD_PROMOTION:UF_CRM_1779488339299}
    stage-publish: ${BITRIX_STAGE_PUBLISH:NEW}
    stage-active: ${BITRIX_STAGE_ACTIVE:EXECUTING}
    stage-closed: ${BITRIX_STAGE_CLOSED:WON}
    stage-archived: ${BITRIX_STAGE_ARCHIVED:LOSE}
