services:
  db:
    image: postgres:16-alpine
    container_name: cian-db
    environment:
      POSTGRES_DB: cian
      POSTGRES_USER: cian
      POSTGRES_PASSWORD: cian
    ports:
      - "5433:5432"
    volumes:
      - cian_pgdata:/var/lib/postgresql/data
      - ../src/sql/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
      - ../src/sql/seed.sql:/docker-entrypoint-initdb.d/02-seed.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U cian -d cian"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  rabbitmq:
    image: rabbitmq:3-management
    container_name: cian-rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: cian
      RABBITMQ_DEFAULT_PASS: cian
    ports:
      - "5672:5672"
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s

  mailpit:
    image: axllent/mailpit:v1.21
    container_name: cian-mailpit
    ports:
      - "1025:1025"
      - "8025:8025"
    healthcheck:
      test: ["CMD", "/mailpit", "readyz"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 5s

  app:
    image: cian-app:latest
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: cian-app
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: docker,api
      DB_HOST: db
      DB_PORT: "5432"
      DB_NAME: cian
      DB_USER: cian
      DB_PASSWORD: cian
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: "5672"
      RABBITMQ_USER: cian
      RABBITMQ_PASSWORD: cian
      MAIL_HOST: mailpit
      MAIL_PORT: "1025"
      MAIL_FROM: no-reply@cian.local
      JWT_SECRET: docker-dev-secret
    depends_on:
      db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      mailpit:
        condition: service_healthy

  worker-1:
    image: cian-app:latest
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: cian-worker-1
    restart: unless-stopped
    environment:
      SPRING_PROFILES_ACTIVE: docker,worker
      DB_HOST: db
      DB_PORT: "5432"
      DB_NAME: cian
      DB_USER: cian
      DB_PASSWORD: cian
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: "5672"
      RABBITMQ_USER: cian
      RABBITMQ_PASSWORD: cian
      MAIL_HOST: mailpit
      MAIL_PORT: "1025"
      MAIL_FROM: no-reply@cian.local
      JWT_SECRET: docker-dev-secret
    depends_on:
      db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      mailpit:
        condition: service_healthy

  worker-2:
    image: cian-app:latest
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: cian-worker-2
    restart: unless-stopped
    environment:
      SPRING_PROFILES_ACTIVE: docker,worker
      DB_HOST: db
      DB_PORT: "5432"
      DB_NAME: cian
      DB_USER: cian
      DB_PASSWORD: cian
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: "5672"
      RABBITMQ_USER: cian
      RABBITMQ_PASSWORD: cian
      MAIL_HOST: mailpit
      MAIL_PORT: "1025"
      MAIL_FROM: no-reply@cian.local
      JWT_SECRET: docker-dev-secret
    depends_on:
      db:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      mailpit:
        condition: service_healthy

volumes:
  cian_pgdata:
